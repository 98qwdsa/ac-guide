<!--src/pages/manage/addRole/addRole.wxml-->
<view class="input-container">
  <input class="input" placeholder-class="search_name_placeholder"
    bindinput="bindKeyInput" placeholder="搜索" value="{{searchInput}}"/>
  <icon type="clear" size="20" catchtap="deleteInputText"/>
</view>
<view class="user-list">
  <block wx:for="{{userList}}" wx:key="{{index}}">
    <user-item userName="{{item.name}}">
      <view class="user-add" bindtap="addUser" data-adduserid="{{item._id}}" 
            data-addusername="{{item.name}}">
        <label>
          <text>添加</text>
        </label>
      </view>
    </user-item>
  </block>
</view>
